<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity" >
    <!--
    <item android:id="@+id/action_settings"
        android:title="@string/action_settings"
        android:orderInCategory="100"
        android:showAsAction="never" />  -->

    <item android:id="@+id/action_add_record"
          android:title="@string/action_add_record"
          android:orderInCategory="100"
          android:showAsAction="always"
          android:icon="@drawable/record_add"
            />

    <item android:id="@+id/action_add_list"
          android:title="@string/action_add_list"
          android:orderInCategory="100"
          android:showAsAction="always"
          android:icon="@drawable/list_add"
            />

</menu>
